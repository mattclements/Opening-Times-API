Opening Hours RESTful API
=========================

This code has been created to allow API Access to opening-hours.co.uk Data using regular exports 
to a MySQL Server which can then be served by a RESTful API
